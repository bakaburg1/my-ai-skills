interface:
  display_name: "Spawn Sub Agents"
  short_description: "Spawn Codex sub-agents for shell-only tasks"
  default_prompt: "Use $spawn-sub-agents to run parallel noninteractive sub-agent probes."
