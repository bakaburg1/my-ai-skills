interface:
  display_name: "Spawn Sub Agents"
  short_description: "Run one-shot noninteractive sub-agents for investigation, testing, or bounded implementation"
  default_prompt: "Use $spawn-sub-agents to delegate a one-shot noninteractive sub-agent task. If the delegated task edits files, provide a full detailed implementation plan."
